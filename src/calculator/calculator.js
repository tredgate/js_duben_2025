export class Calculator {
  add(a, b) {
    return a + b;
  }

  subtract(a, b) {
    const result = a - b;
    return result;
  }

  multiply(a, b) {
    const result = a * b;
    return result;
  }

  divide(a, b) {
    const result = a / b;
    return result;
  }
}
